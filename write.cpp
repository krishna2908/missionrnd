/*#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
	FILE *outfile;
	outfile = fopen("person.dat", "w");
	fwrite(&, sizeof(struct person), 1, outfile);
	fwrite(&input2, sizeof(struct person), 1, outfile);

	return 0;
}
*/